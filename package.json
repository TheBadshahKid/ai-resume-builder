{
  "name": "ai-resume-builder-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client && python3 -m pip install -r parser-service/requirements.txt",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:parser": "python3 parser-service/app.py",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:parser\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
